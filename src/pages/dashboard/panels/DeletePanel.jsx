import React from 'react';
import styled from 'styled-components';

const DeletePanel = (e) => {
    return(
        <header>
            <h2>Delete</h2>
        </header>
    );
}

export default DeletePanel;
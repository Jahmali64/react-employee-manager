import React from 'react';
import styled from 'styled-components';

const AddPanel = (e) => {
    return(
        <header>
            <h2>Add</h2>
        </header>
    );
}

export default AddPanel;
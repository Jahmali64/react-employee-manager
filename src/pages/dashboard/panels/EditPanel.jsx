import React from 'react';
import styled from 'styled-components';

const EditPanel = (e) => {
    return(
        <header>
            <h2>Edit</h2>
        </header>
    );
}

export default EditPanel;